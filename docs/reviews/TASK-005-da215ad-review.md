# TASK-005 增量独立复核

- 结论：**passed**。
- 日期：2026-09-10。
- 本轮最终受审 HEAD：`da215ad9b22e5d831c0d7a319885ddf7ad3ec8ef`。
- 修复实现：`df92e476025c612d64eb7f474aaad078602d5605`。
- 上轮审查基准：`6d2a81846aa515e8c69f028d68f619d40b502292`。
- 输入：silicon-task005-review(1).zip、result(8).md、上轮源码和独立报告。
- 范围：R1/R2/C1 修复及其直接影响。未修改受审实现，未开始 TASK-006。

## 问题关闭

| 项目 | 结论 | 依据 |
| --- | --- | --- |
| R1：实际已选可选件漏检 | 已解决 | 报价服务将当前选中行单独适配为兼容检查输入，类别完整性统计全部已选行；原模板必选类别仍保留。返回技术行的 required 元数据与目录 checks 实现未改。 |
| R2：新建相同草稿复用幂等键 | 已解决 | 显式新建、成功打开已有草稿时重置保存命令身份；保存签名包含创建/更新目标；失败重试保持原身份。服务端幂等与 expected_version 未改。 |
| C1：重算失败仍显示旧试算 | 已解决 | 重算开始清空当前 calculation 并显示计算中，失败提示待重算，历史金额明确标为历史保存结果，未保存编辑保留。 |

本轮未发现新的阻断项。

## Reviewer 独立验证

- SHA256SUMS：471 项通过；重新生成接口后再次检查。
- 阅读服务/前端增量、新增真实 PG/API 用例、React DOM 组件测试、测试故障夹具及其独立启动路径、验证汇总和浏览器数据库核对证据。
- `npm run typecheck`：通过。
- `npm run build`：通过；23 modules，index-CO6Caif1.js，CSS 仍为 index-CevFy-dt.css。
- `node --test apps/web/tests/*.test.ts`：19 passed、0 failed。
- `infra/check_docs.py`：通过，190 个本地链接，包含旧报告保护校验。
- 重新导出 OpenAPI 和生成客户端类型：与本轮原生成物 SHA-256 一致。
- 定向服务探针加载实际修复后的模块，确认已选可选不匹配 CPU 返回 BLOCK、取消选择后返回 PASS 且不再计价，返回行仍保留原 required=False。
- 定向组件事件探针加载实际修复后的 TSX，确认两次有意新建相同内容使用不同创建幂等键；重算失败后 calculation 为 null 并显示错误。
- 查看桌面失败状态与移动端历史金额截图：当前试算与历史保存结果清楚区分，原页面风格保持，无新增明显视觉阻断问题。

探针复用了上一轮验证方式：服务仓储/客户依赖以及组件 hooks/传输为替身。它们执行实际业务函数/事件处理器，不等同于真实 PostgreSQL、React DOM 或浏览器端到端测试。已安装依赖复用上一轮锁定环境，本次明确加载 review005fix/source 代码。

## 新增回归与证据核对

新增 PG/API 测试覆盖：必选匹配 CPU 与已选可选不匹配 CPU 混合；排除后兼容和金额恢复；可选内存不匹配；可选电源容量 WARN/PASS 与排除后缺失；可选 CPU 满足类别完整性；原必选 GPU 类别保持；included 不重复计费、历史 BOM 不变；独立创建、同键重放和第二份编辑隔离。

新增 React DOM 测试确实挂载 QuoteStudio，操作真实按钮和表单，仅替换 HTTP 边界。三个用例涵盖连续相同新建、打开后再新建、创建成功响应丢失重试、编辑隔离、重算进行中/失败/成功恢复及编辑保留。该测试不属于 Node 的 19 项测试，不能混合计数。

独立测试夹具通过显式 test 工厂在真实应用成功返回后替换响应；正常生产入口没有加入故障控制接口或权限绕过。浏览器 PG 核对证据显示相同请求内容的两次独立创建对应不同 draft_id，另一次响应丢失创建保持单份记录。

执行者报告：完整后端 **92 passed、0 skipped、3 warnings**；真实 React DOM **3 passed**；定向 HTTPS 浏览器 R1/R2/C1 完成，保留六张截图。以上为执行者交付证据，不计为 Reviewer 独立重跑。

## 验证边界

当前环境没有可用的 PostgreSQL 17.11 / Java 21 / Keycloak 测试栈；此前 PostgreSQL 构建受缺少 bison/flex 限制。本轮未重跑真实 PG/API/Worker/OIDC、真实 React DOM 测试页或完整浏览器流程。未执行双标签浏览器竞态、远程 CI、容器运行或生产部署。

审查包不含 .git；提交身份与祖先关系依据固定清单和交付差异，未访问用户本机仓库或远程核验。哈希通过证明包内一致性，不独立证明 Git 来源真实性。局部截图检查不替代完整视觉基线验收。

真实销售、税费及完整兼容政策仍未配置，sale_ready=false；本轮通过仅代表 TASK-005 草稿范围的审查问题已解决，不构成正式报价发布或生产可用认证。

## 交接

TASK-005 可完成验收交接。将本报告原样新增到 `docs/reviews/TASK-005-da215ad-review.md`，保留原 changes_requested 报告及历史证据；由负责人确认后同步 accepted 与验收引用。TASK-006 应依据另行明确发布的任务书和授权启动，本轮复核未代改用户仓库状态。
