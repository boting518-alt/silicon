# TASK-008 增量独立复核报告

结论：**passed**（R1/R2 修复范围）。原 P1/P2 已解决，未发现新的阻断问题。建议负责人确认 TASK-008 accepted；本报告不修改仓库状态，不代表生产部署验收。

## 审查对象

- 输入：silicon-task008-review(1).zip、result(20260911-055008).md。
- 修复基准：dd4031fb6950e28446855131642cc01e54095991。
- 实现：4e625e42954fcabf774c8f3994eb924cf133b0ce。
- 最终 HEAD：b56ed65f2a234a956f75102b1320f7658dddfd71。

提交及工作树信息来自清单，未独立核验远程/Git 对象库。审查了增量源码、共享引用查询、锁顺序、上传流程、新增测试和交付证据；未修改被审查实现。

## 问题闭环

| 原问题 | 结论 | 依据 |
| --- | --- | --- |
| R1 / P1：清理误删有效采购/导入原件 | 已解决 | clean_contract_files 联合读取 contract_files、signed_files、inv_attachments、inv_imports；保留既有 storage_id。全部领域锁取得后才读取引用，缺表或查询失败在删除前终止，保留 24 小时及在途文件锁保护。 |
| R2 / P2：上传先无界读取后鉴权 | 已解决 | 上传先完成授权与对象核对，释放事务后流式计数；超限立即 413，不继续消费后续块。正式写入时再次授权并检查版本/冻结状态；文件锁保持到事务退出，重复上传副本回收，提交不确定时保留待引用感知清理。 |

清理锁序为身份/membership → catalog 共享 → quote 排他 → inventory 排他。库存路径没有反向获取 quote 锁；此次范围内未发现新增锁序循环。引用查询包括仍有效的全部导入状态，避免把尚未提交预览当成孤儿。

修复仅涉及相关服务整合、维护脚本、测试及交付资料；无新增迁移、依赖升级或产品 UI 改动。旧清理版本的误删风险仍存在，应使用本次修复版本。

## 本次独立验证

| 检查 | 结果 |
| --- | --- |
| SHA256SUMS | 903 项通过 |
| npm run build，含 tsc --noEmit | 通过 |
| Node 前端测试 | 19 passed |
| infra/check_docs.py | 通过，256 个本地链接及任务状态等检查 |
| OpenAPI 与客户端再生成 | 通过，逐字节一致 |
| R1 定向探针 | 真实临时 UUID 文件超过 24 小时仍保留；确认查询包含采购附件及导入引用 |
| R2 定向探针 | 未授权读取 0 块；超限在第 2 块拒绝，不读取第 3 块 |
| 截图人工查看 | 查看期初关闭移动图及库存对账桌面图，延续原风格 |

探针执行真实清理函数/上传 endpoint，但数据库、授权及请求流采用受控替身；不等价于真实 PG/API 并发验证。上传探针初次因替身遗漏 require 方法失败，补齐探针后通过，未修改产品代码。

使用当前 Python 3.12.14 与既有包目录、既有 Node 安装，未执行全仓库干净安装，也未宣称严格复现执行者 Python 3.12.7 环境。

## 提交证据核对及限制

新增 test_inventory_files.py 的 8 项真实 PG/文件/API 用例覆盖：

- 合同、采购、收货、预览/已提交 CSV 原件保留及 hash/下载；只回收孤儿和可回收已删除文件。
- 缺表保守失败、跨租户、在途 flock、上传提交期间清理等待及提交后引用可见。
- 无 Content-Length 的实际 ASGI 分块读取、未授权不消费、超限提前停止。
- 上传期间撤权/冻结、限额边界、幂等重放及企业上下文变化。

提交方记录定向 **8 passed / 2 warnings**，完整后端 **192 passed / 8 warnings**，包含此前 184 项。实际 React+HTTP 替身 2 项另计；不得与真实数据库测试混为一类。

提交方补查真实浏览器上传拒绝、合法文件、冻结、临时根清理、采购时间线、关闭期初预览和对账。浏览器下载点击有服务端 allowed 审计，但工具未返回落盘文件，因此下载落盘 checksum 为 **not_run**；PG/API 下载字节及服务端 hash 校验是另一类证据，不能替代它。

本次没有独立重跑完整 PostgreSQL/IdP、React DOM 或真实浏览器流程；相关结论结合实现、定向探针及提交证据。旧失败日志与历史报告保留，文件名中的 red/green 不作为测试结果判断依据。截图目标视口与导出尺寸差异已披露。

远程 CI、Docker 实测、其他浏览器、生产部署及真实业务政策未被本报告认证。此前供应商页面、销售来源选择、仓库分组等产品限制继续保留，不因本次修复而视为完成。

## 交接建议

将本报告原样新增到 docs/reviews/TASK-008-b56ed65-review.md，保留首次 changes_requested 报告。负责人确认 accepted 后更新任务状态，按照 TASK-009 任务书核实实际基线再启动后续工作。
