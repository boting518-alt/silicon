# TASK-012 R1 增量独立审查报告

- 日期：2026-09-12
- 结论：**passed**
- R1（P2）：已关闭；本次增量范围内未发现新的阻断问题。
- 审查基线：`3907e6fa104d5910394f64249f1b12ab1578141e`
- 修复实现：`694064e741740b34d75e23051579090b2db247cd`
- 最终 HEAD：`18966f4eaf3fdd1311b883e32f6f8e2eb123d48a`
- 输入：`silicon-task012-review(1).zip`、`result(20260912-004308).md`

基于导出源码、增量差异、测试源码及交付证据审查。提交号、分支与导出状态来自交付清单，未通过 Git 对象或远程仓库独立核验祖先关系。

## R1 关闭依据

原处置要求整条隔离余额等于单次返回数量，导致同层同库位分批返回并隔离后无法逐条处置。

新增 `custody_balance()` 将明细额度与聚合库存分开：先从不可变移动记录验证目标层、状态、数量及唯一保管库位，再要求该库位余额大于等于本次明细数量。`dispose()` 根据原换件或 hold 检验移动定位，`rma_inspect()` 根据返回移动定位，避免检验阶段先消费错误库位。

原租户/工单关联、hold 前置、唯一处置、expected_version、命令幂等及库存事务锁继续保留。允许同库位余额 2 按一条返回的 1 件消耗，不允许借用另一库位余额。移库后原库位不足时，需要受控移回；本轮未新增自动跨库位分配。

阅读新增 `test_service_disposition.py` 四项测试，覆盖：同一行两次返回与 hold 后归还/报废、余额 2→1→0、客户所有权和公司库存成本保持、原返回历史不变、重复和重放、同/不同明细并发、多库位、余额不足、未 hold、跨工单/跨租户、撤权重放。

## 独立验证

| Reviewer 本次执行 | 结果 |
| --- | --- |
| SHA256SUMS | 1,462 项通过，证明包内文件与清单一致 |
| 原审查报告 | 归档文件与原报告逐字节一致 |
| API 契约基线比较 | OpenAPI 与生成客户端均与上轮已验证版本逐字节一致 |
| 前端类型检查与构建 | npm run build 通过，包含 tsc --noEmit |
| Node 前端测试 | 19 项通过 |
| 文档检查 | 397 个本地链接及交接检查通过 |
| 真实 helper 定向探针 | 同库位余额 2 满足明细 1；仅其他库位有余额时拒绝 |

定向探针调用实际 custody_balance，持久化读取使用替身，不是 PostgreSQL/API 测试。前端复用已有依赖目录，未执行全新安装。本次未独立重跑 PG、迁移、IdP、Worker、React DOM 或浏览器业务闭环。

## 执行证据及限制

执行者报告完整后端 **262 passed / 21 warnings**，包含原 259 项和最先 3 项修复测试；第 4 项在该轮收集后补充，由最终专项 **4 passed / 3 warnings** 覆盖。不能把它们相加声称完整全套 266 项通过，也不声称已全套运行最终 263 项。

执行者另报告实际 React + HTTP 替身 4 项通过，真实浏览器完成两次返回、分别 hold、一条归还客户及另一条报废，刷新与库存对账通过。这些为执行者证据，不是 Reviewer 独立运行结果。

无迁移或契约变更，产品前端未改。原人工/其他费用暂无独立冲销入口等产品限制保持。远程 CI、Docker、其他浏览器和生产环境未独立验证；本结论不等于生产部署验收。

## 交接建议

建议业务负责人接受 TASK-012。新增本报告至 `docs/reviews/TASK-012-18966f4-review.md`，保留原 changes_requested 报告和历史验证记录，再确认 accepted 状态。TASK-013 按单独授权推进。

Reviewer 未修改被审查实现，未部署，未开始下一任务。
