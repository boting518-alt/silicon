# TASK-006 增量独立复核

结论：**passed**（本次 R1/R2 修复范围）。未发现新的阻断问题。建议产品负责人将 TASK-006 确认为 accepted；本报告不修改仓库状态、不启动 TASK-007，也不代表生产发布批准。

## 审查对象

- 输入：silicon-task006-review(1).zip、result(10).md。
- 修复基线：0ca955af10115b1fb7369885c348465672b22dc9。
- 实现提交：5183b2a7d080269bdf951b41d6a4198bd3808a77。
- 最终 HEAD：59f55c813aba62ec8f18dc5d5e7cc649b277aeca。
- 提交标识及工作树状态来自交付清单；压缩包不含 Git 对象库，本次未独立验证远程提交或 CI。
- 本次复核修复差异、相关调用路径、回归测试及证据，未修改被审查实现。

## 问题闭环

| 原问题 | 结论 | 依据 |
| --- | --- | --- |
| R1 / P2：已发布草稿可再次提交并产生新根报价 | 已解决 | publication/service.py 的 submit 在创建候选前调用 require_unpublished；issue 在同一候选已发布的重放分支之后、首次发布前再次检查。路由仍在现有事务与报价写锁内执行；成功命令重放保留原结果。QuoteDetail 暴露 published_version_id，前端显示独立修订说明。 |
| R2 / P2：不同来源版本使用相同幂等键会共用修订草稿或冲突 | 已解决 | quotes/service.py 将内部保存操作按 quote.revise:<source_version_id> 隔离；来源关联随草稿 INSERT 原子写入。publication.revise 删除了事后覆盖来源的 UPDATE，同来源重放不重新指定来源。 |

补充核对：issue 对同候选不同键的重复调用仍返回既有发布对象，并复核参与者权限；不同历史候选不能绕过已发布草稿限制。修订入口在重放前仍检查当前权限和来源可见性。此次没有新增数据库迁移或更改旧发布快照结构。

## 本次独立执行

| 检查 | 结果 |
| --- | --- |
| SHA256SUMS 全清单校验 | 598 项通过 |
| npm run build（含 tsc --noEmit） | 通过 |
| node --test apps/web/tests/*.test.ts | 19 passed |
| python3 infra/check_docs.py | 通过，210 个本地链接及任务状态等检查 |
| OpenAPI 导出及 npm run api:types | 通过；生成前后两个契约文件逐字节一致 |
| R1 服务层探针 | 已发布草稿在 INSERT 前返回 PUBLISHED_DRAFT_REQUIRES_REVISION |
| R2 服务层探针 | 不同来源同键同配置产生不同草稿；各自重放稳定；无来源覆盖 UPDATE |
| 桌面与移动截图人工查看 | 独立修订说明可见，延续现有样式 |

服务层探针执行真实生产函数，但替换了持久化、权限和计价边界，不等价于真实 PostgreSQL、HTTP 或并发测试。构建复用了本地既有依赖安装，本次未重新执行干净依赖安装。

## 提交方证据核对

提交包记录真实数据库/API 后端测试 **124 passed、4 warnings**，随后对补充的同键发布重放断言执行 **1 passed、30 deselected**。后者是定向复跑，不应累计描述成 125 项独立测试。

新增测试涵盖：已发布草稿新键再次提交、原命令重放、同候选重复发布、发布与再次提交竞争、遗留候选发布拦截；不同来源同键同/不同配置、并发创建、长键、租户及权限变化、各来源 R2 编号和原内容保留。初次全量运行的 fixture 建立顺序失败单独保留，并明确区别于缺陷 red 证据。

提交方还提供实际 React 组件 5+3 项测试及真实登录审批、发布、独立修订再审批的浏览器证据。截图实际尺寸为 1425×891、375×812，记录了与目标视口的导出尺寸差异。截图仅用于此次修订提示的视觉核对，不视为完整响应式页面验收。

## 验证边界与交接

本次没有在当前审查环境独立重跑真实 PostgreSQL/IdP 全套测试、React DOM 组件测试或浏览器流程；当前环境缺少适用的数据库及 IdP 运行条件。相关结论结合实现检查、测试代码和提交日志作出。Docker、其他浏览器、远程 CI 和生产部署没有被本报告认证。

可将本报告新增到 docs/reviews/TASK-006-59f55c8-review.md，保留上一份 changes_requested 报告。确认 accepted 后，依照已批准的 TASK-007 任务书和前置条件继续；不要把后续任务或部署视为已完成。
