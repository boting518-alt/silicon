# TASK-007 增量独立复核

结论：**passed**（R1/R2 修复范围）。两项 P2 已解决，C1 最终源码截图已补齐。本次未发现新的阻断问题。建议负责人确认 TASK-007 accepted；不代表生产部署验收。

## 审查对象

- 输入：silicon-task007-review(1).zip、result(20260910-172258).md。
- 修复基准：732089f2e812920c2e20b863cf9749b4dd6dad3e。
- 修复实现：9b79adf11fa31ec22e4ef27a00cf1847464d8673。
- 最终 HEAD：4802fcccb739fa89cada3be5179e21e0066121a0。
- 提交身份、祖先关系及工作树状态依交付清单；未独立验证 Git 对象库、远程或 CI。

复核修复差异、相关测试、锁与附件生命周期、解码资源限制及交付证据，未修改实现。

## 问题闭环

| 问题 | 结论 | 核对依据 |
| --- | --- | --- |
| R1：签约前未关联文件被永久保留 | 已解决 | associate 允许删除已签合同中非补充的 pending 文件，仍检查 signed_files 引用。删除不提升已签 workspace 版本，旧 pending 关联仍阻断；前端开放删除并说明补充材料处理方式。 |
| R2：标记正确但内容无效的证明文件被接受 | 已解决 | 独立进程执行 PDF 逐页渲染、JPEG/PNG 验证及完整加载，限制页数、像素、CPU、内存和时间；上传、关联和新签约重检未冻结文件，保留已签事实及原字节。 |
| C1：截图早于最终样式 | 已补齐 | 新增四张最终修复源码截图，桌面/移动各覆盖格式错误与旧 pending 清理状态；旧图保持。 |

R1 保持既有合同写锁、当前权限和幂等作用域。新增真实 PG 测试覆盖删除、24 小时清理、原证明/订单不变、撤权重放、跨租户拒绝与签约竞争。

R2 固定新增 pypdfium2 5.13.0、Pillow 12.3.0、psutil 7.2.2。新增反例覆盖伪造 PDF/JPEG、缺 IDAT 的 PNG、截断文件、旧未冻结文件重新验证以及 PDF 页数/像素限制。格式解码不是病毒扫描、签名认证或操作系统安全沙箱，交付正确保留这些限制。

## 本次独立执行

| 检查 | 结果 |
| --- | --- |
| SHA256SUMS | 743 项通过 |
| npm run build（含 tsc --noEmit） | 通过 |
| Node 前端测试 | 19 passed |
| infra/check_docs.py | 通过，231 个本地链接及任务状态等检查 |
| OpenAPI 与客户端再生成 | 通过；逐字节一致 |
| R1 真实服务函数探针，数据库边界替身 | 通过：允许删除 pending、不 bump 已签版本、禁止关联旧 pending、保护 signed_files 引用 |
| 锁定版本的独立解码进程 | 7 项通过：拒绝原无效 PDF/JPEG 与缺 IDAT PNG；接受有效 PDF/JPEG/PNG；超 50 页返回资源限制 |
| 截图人工查看 | 查看桌面 pending 删除与移动格式错误图；控件样式、说明及对应操作可见 |

前端构建复用既有安装；新增三项 Python 解析依赖按本次锁定版本安装后完成解码验证。未进行全仓库干净安装。

### 完整 Store 入口验证限制

本次尝试执行真实 Store.validate（包含父进程 RSS 监测）。当前工具运行环境中，Popen 返回的部分子进程 PID 无法被 psutil.Process 查询，发生 NoSuchProcess，随后返回 FILE_VALIDATOR_UNAVAILABLE；因此不能将完整 Store 入口列为独立通过。

该现象出现在父进程向解码子进程输送输入之前，不能据此断言是文件格式逻辑失败。直接使用同一 Python 和锁定依赖启动真实 validate_file.py、保持空环境变量后，上述 7 项均通过。报告保留完整入口的失败与环境限制；未改动产品以绕过 RSS 检查。完整父进程监控在目标原生环境的通过证据来自提交方，本次不宣称独立复现。

## 提交方证据与验证边界

提交方记录完整后端 **164 passed、7 warnings**，随后另增资源限额用例定向 **2 passed、40 deselected**。这不是完整 166 项套件重跑。合同 40 项及后续 2 项均有对应日志。

提交方还记录实际 React 组件合同 6、发布 5、报价 3 项（HTTP 替身），以及真实 HTTPS/IdP 浏览器上传拒绝、有效文件关联、签约、旧 pending 删除、重新载入及订单查询。浏览器会话失效后重新登录的前后记录保留。

本次未在当前环境重跑完整 PostgreSQL/IdP、React DOM 或真实浏览器流程，相关结论结合源码、定向探针与提交证据。截图目标视口 1440×900/390×844，实际导出 1425×891/375×812，差异已披露，未将导出尺寸等同于目标视口。

Darwin RLIMIT_DATA 不兼容的失败诊断、旧测试结果及原报告保持；当前采用父进程 RSS 监测及 Linux 地址空间限制。Docker、其他浏览器、AV、远程 CI 和生产政策仍未被本报告认证。

## 后续

将本报告原样新增到 docs/reviews/TASK-007-4802fcc-review.md，保留首次 changes_requested 报告。负责人确认 accepted 后更新任务登记，按 TASK-008 任务书核实实际基线并继续；本次未修改任务状态或开始 TASK-008。
