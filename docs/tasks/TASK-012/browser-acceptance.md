# TASK-012 浏览器验收

真实Codex内置浏览器、可信开发HTTPS、Keycloak26.7.3、独立PG17.11；虚构身份和数据。没有关闭证书校验或变更系统信任。截图继承原硅屿侧栏、绿色token、字体与卡片；售后工作台指标/工单/责任、交接、备件、RMA、费用分区是新增业务内容，不冒称原Demo已有页面。

## 实际完成

1. 真实IdP登录 → 企业A → 在实际已发货DEL-1上创建SV-BROWSER-012。
2. 返修实物收件，客户所有权保持；登记诊断、方案与人工依据。
3. 从虚构PUB-H库存预留1件 → 领用 → 手工同型号兼容确认 → 追加换件，旧客户件隔离。材料消耗80.00 CNY；不再次计销售出库。
4. 先为旧件建立RMA-BROWSER-012责任 → 人工复测 → 待验证 → 客户确认解决 → 实际归还 → 关闭。
5. 关闭后的RMA仍可送收：实际送供应商 → 原件修复返回，待检1件 → 检查通过并按客户所有物归还，待检归零。
6. 建立SERVICE-BROWSER-012服务收费来源100.00 CNY → 资金工作台选择“客户服务”来源 → 应收计划草稿/确认 → 虚构实际收款登记/确认 → 核销100.00。
7. 刷新、重新打开售后与RMA；独立API重载后原事实仍存在。资金刷新显示有效100.00、已核销100.00、未结0.00。
8. 切企业B后工单、设备和旧表单清除。曾发现负责人查询缺少企业过滤，修复后再次实际查询B只显示其成员，红绿PG证据保留。
9. 从设备详情的独立售后摘要按钮进入售后工作台。退出后不显示受保护记录；多次真实会话到期要求重新认证，未绕过。
10. 1440×900、390×844保存并人工检查。初次样式漏用既有表单/网格类，修复后保存final截图，原初稿截图保留。

前置首轮用完备件，页面正确无库存；修正虚构种子显式期初导入后重建栈再做闭环。截图固定显示时钟沿原测试配置2026-09-08；后端真实时间不同，用户登记时间现在作为表单字段固定以支持丢失响应重试。真实业务不依赖截图时钟。

## 分开记录的组件测试

`/tests/service-component.html`：真实React StrictMode、HTTP替身，4项通过：初始化+创建响应丢失重试和新意图、延迟成功响应跨企业丢弃、窗口激活撤权清除、独立售后摘要拒绝不破坏基础设备。创建时间每次生成导致key变化的红测与修复绿测均保存。它不替代PG并发/权限和上述真实浏览器闭环。

## 限制

未在真实浏览器逐一重放所有SN换新、并发、隔离后报废、取消重开和成本撤权分支；相关PG/API与组件覆盖分别列于validation。未运行其他浏览器引擎、Docker、生产或真实供应商/银行/硬件集成。未新增附件上传，不宣称验证任何新下载入口。完整复现条件见 [实施说明](implementation.md)。

截图清单与哈希见 [screenshots.json](evidence/screenshots.json)；真实DOM记录、红绿与启动日志均在evidence目录。最终桌面为desktop-final.jpg，移动为mobile-final.jpg；desktop-service-finance.jpg为资金核销页面。

视口与图片尺寸分开登记：本轮实际调用浏览器viewport设置1440×900、390×844；同一工具随后通过空白页面DOM再次核验CSS尺寸完全一致。导出的JPEG分别为1425×891、375×812，不能把图片像素尺寸当作CSS视口。见 [视口核验](evidence/viewport-verification.json) 与截图清单的image_pixels字段；未对业务截图做编辑或补画。
