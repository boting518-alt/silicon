# TASK-012 执行结果

status: review_ready。本报告不代表独立验收。

已验收基线：`6d89a1023d06b795686bfbc1504da7dde0baf099`。
验收交接：`a7072825c8b1885ad7d5320471d13c5613470af7`。
领域决策：`5885e174916cec02b4ae4d6bcdf2e1f777923332`。
实施提交及最终HEAD在收尾登记与审查包manifest精确记录，避免自引用提交号。TASK011原结果与独立报告保持原样；Reviewer未独立重跑PG/OIDC/完整浏览器等限制没有改写。

## 已实现

- 独立service模块：已交付稳定设备关联售后、远程/现场/返修、责任人/保修依据/状态与历史、客户实物收回归还、维修后人工测试。
- 共享预留与库存移动：与装配互斥，不把客户代管或隔离件当可用自有备件。领用后仍在库存；实际安装消费材料，未用件退回待检。旧安装只闭合有效期，后继复用稳定槽位；有后续事实时阻止更正。
- 客户旧件归属和当前去向、RMA多行/部分返回、原SN修复/新SN换货、返回待检、显式处置及有依据转自有备件。未知成本不当零，未知资料保留UNKNOWN并要求人工确认。
- 人工/其他费用、客户服务和供应商维修收费来源。服务收费不虚增销售/采购合同；资金计划、实收实付与核销分别通过既有资金模块登记。
- 独立售后权限与成本权限、全局成员显式租户筛选、当前页面上下文校验、幂等重放前权限复查、聚合事务锁和不可变数据库事实。设备详情的售后增强失败不会清空基础设备。
- 0015增量迁移、OpenAPI和客户端类型；售后工作台指标/工单/备件/RMA/费用及设备入口。原设计token与卡片/侧栏沿用，未改原Demo。

设计及复现见 [ADR-017](../../architecture/adr/ADR-017.md)、[实施说明](implementation.md) 和 [浏览器证据](browser-acceptance.md)。

## 验证与修复过程

真实PG红绿分阶段保留：初始工单/备件端点失败后实现通过；RMA查询误用无id的余额表已修复，RMA+资金23项通过；换件逆向4项通过；扩展售后8项、前序领域60项、售后SN/RMA等后续专项结果分别保留。

发生过测试准备错误：RMA测试参数拼写导致collection错误、首次服务路由缩进错误、新增处置表漏入测试清理清单、共享预留测试误用装配请求格式；这些不计作业务缺陷红测。实际API缺陷RMA余额排序与人员跨企业列表、页面创建重试时间变化，以及初次样式类缺失均修复。原始输出未覆盖；需要规范差异空白的日志另存gzip原字节。

真实浏览器完成售后收件→换件→复测→归还关闭→旧件RMA送收检查处置；服务100.00收费→应收→实收→核销，刷新后未结0.00。企业切换后无旧企业工单，人员筛选修复后重新实际查询；设备详情入口与退出验证完成。1440×900与390×844截图见 [清单](evidence/screenshots.json)。多次IdP会话到期按真实认证重新登录，未延长超时或绕过认证。

实际React+HTTP替身4项与真实PG、真实浏览器分别记录；19项Node回归通过。完整后端、最终类型/构建/契约/文档结果追加于下。精确命令与日志索引见 [validation.json](evidence/validation.json)。

## 明确限制

- CNY、整数件数；人工保修与兼容确认、人工收费/成本依据，不推定真实厂商政策、税率或免费服务。
- 已确认费用/收费为冻结事实；本期不提供无依据改金额或供应商线上受理。人工/其他费用暂无独立冲销入口，需要明确后续业务更正规则；不删除历史事实。服务来源不可用合同额度调整虚增。
- 换件更正限定无后续依赖、旧件仍在本方且可完整还原；客户已经拿走、供应商送修或新安装被后续替换时阻止直接更正。
- 原件修复、新SN替换和转自有须当前所有权/成本依据；不把客户所有件直接当本方免费备件。RMA检验选择继续隔离时仍保留实际隔离状态与最终处置入口。
- 使用现有稳定设备范围，未扩展没有设备档案的第三方整机路径。没有硬件采集Agent、供应商平台、远控、银行、税务或生产部署。
- 报告为档案编号登记；未新增上传/签名URL，原共享文件引用保护未改。原TASK008文件校验环境限制仍保留。
- 未逐项在真实浏览器复放所有并发/逆向/换新/隔离后处置分支；PG/API覆盖与组件覆盖明确分开。其他浏览器、Docker、真实外部系统为not_run；远程CI未核实。
- 读写依旧使用租户级库存事务锁，未做大规模吞吐量测试；不等同于生产上线就绪。

只执行TASK012，不开始TASK013。完成后等待独立Reviewer。


## 最终验证

完整后端 **259 passed / 15 warnings / 326.23s**，包含真实OIDC和隔离PG；16项售后专项 **16 passed / 2 warnings / 40.76s**，包含在完整259项内。旧领域定向60项是阶段回归，不与259重复加总。首次完整257通过、1失败的测试夹具前置错误已修复，旧日志保留。

实际React+HTTP替身 **4 passed**；Node **19 passed**。最终类型检查、构建、OpenAPI/客户端重复生成一致性、文档及git diff --check通过。真实浏览器闭环、企业切换、设备入口、退出与双视口证据如上；远程CI未核实。

变更清单见 [changed-files.txt](changed-files.txt)。审查包从最终HEAD完整导出，校验源码Git blob、基线增量补丁重放、历史报告/证据不变及解压SHA256SUMS。执行者不自行accepted，不将推送等同于独立审查通过。

实现提交：`b8dcbc663be23196f6179532deedd64057376c7e`。本收尾提交仅登记实现引用；最终完整HEAD以审查包REVIEW_MANIFEST.md为准，源码按该提交完整导出。

## R1 独立审查修复（2026-09-12）

status: review_ready，等待独立增量复核；不自行 accepted。

修复前基准：`3907e6fa104d5910394f64249f1b12ab1578141e`，main，开始时工作树干净，无后续提交。用户已明确授权修复。原 [独立报告](../../reviews/TASK-012-3907e6f-review.md) 原样归档，原报告结论、以上执行历史及原证据保留。

原因：同层同库位聚合余额可包含多条返回，原处置错误要求其恰等于一条返回数量。修复将未处置明细额度与库存可用余额分开，并以不可变返回/检验/换件移动确定实际保管库位；不跨库位借用余额。租户、工单、hold、唯一处置、版本、幂等和原事务锁保留。新 helper 同时用于返回检验，避免检验先错用其他库位。详见 [ADR-017](../../architecture/adr/ADR-017.md)。无迁移或契约变更，未修改产品前端/视觉。

真实红测：经完整采购/装配/发货/换件/RMA API 前置，同批次2件分两次各返1并hold，第一次处置报 SERVICE_PART_DEPENDENCY；修复后成功按2→1→0消费。最终4项新增真实PG/API测试覆盖独立归还/报废、客户所有权及成本不混淆、旧返回历史不变、重放/重复、同明细及不同明细并发、库位绑定、余额不足、未hold、跨工单/跨租户和撤权后重放。

验证：完整后端 **262 passed / 21 warnings / 337.65s**（包含原259与最先3项R1；收集后新增的第4项由专项覆盖）；最终专项 **4 passed / 3 warnings / 14.51s**，不重复加总。完整回归包含真实OIDC、隔离PG、库存/装配/交付/资金/Worker及空库和0014升级测试。Node **19 passed**、真实React+HTTP替身 **4 passed**；类型、构建、契约字节一致性、文档及差异检查通过。

真实浏览器完成两次原件返回、分别hold、一条归还客户和另一条报废，刷新查询持久化与库存对账差异0。桌面1440×900和移动390×844截图及工具像素差异见 [浏览器记录](evidence/r1/browser-acceptance.md)。仅使用隔离PG/IdP/虚构数据与已有可信证书，未更改系统信任、常驻库或Demo。

准确命令、原始红绿日志、夹具准备错误及未运行项见 [validation.json](evidence/r1/validation.json)。首次stock字段错误及尝试逆向已有下游移动的测试准备错误均单独保留，不算R1红测。移库后原库位不足时需先受控移回；不新增自动库位分配。原人工/其他费用暂无独立冲销入口等限制继续有效。远程CI未核实，Docker/其他浏览器/生产外部系统not_run。

修复提交及最终HEAD在后续登记和审查包manifest记录；增量包从最终commit完整导出，基于本轮受审提交产生diff，验证历史证据原样、源码blob和解压校验和。保持review_ready，不开始TASK013。
