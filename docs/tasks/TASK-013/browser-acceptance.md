# TASK-013 真实浏览器验收

本地执行日期2026-09-12。使用已有受信任 localhost HTTPS、真实 Keycloak26.7.3、隔离 PostgreSQL17.11、实际API和Vite；没有关闭TLS或改系统信任。没有操作常驻数据库。浏览器日志不包含会话或令牌。

## 可重复准备

从仓库根安装锁定依赖，参照 README 和迁移说明。设置 `SILICON_TEST_PG_BIN` 为测试PG的bin目录、`SILICON_TEST_KEYCLOAK_HOME` 为已验证Keycloak目录，Java21在PATH。准备 `.tools/tls/localhost.crt`、`localhost.key` 和 `.tools/visual/PingFang.ttc`，它们不在包中；Reviewer建立自己的证书信任需另外授权，不能忽略证书错误。

运行 `SILICON_BROWSER_ANALYTICS=1 SILICON_BROWSER_CONTRACTS=1 .venv/bin/python infra/browser_stack.py`。夹具仅调用隔离环境业务命令；退出时停止自己的临时集群、IdP和服务器并清理目录。不要与OIDC集成同时占用Keycloak端口。

## 已执行真实操作

1. 打开 https://localhost:5173，经真实登录页使用虚构 alice / Fictional-alice-17!；选择企业A。
2. 期间2026-01-01至2026-09-13（不含），截止2026-09-12。本轮页面装饰时钟固定2026-09-08，后端时钟未伪造。看到签约360.00、合同1、净交付1、实收80.00、应收110.00。
3. 地图切换客户数、净交付、签约；浙江客户2家，净交付1、签约360.00。点击浙江筛选，再查看地区贡献明细，CON-001贡献360.00。
4. 点击“打开原业务”，进入原合同组件并打开真实已签约冻结v2；付款约定、来源及只读状态正常。返回驾驶舱重新设置期间。
5. 应收账龄下钻：60.00到期2026-09-01，50.00质保金待释放/未知到期，不计逾期。
6. 截止改2026-09-08，库存历史5件/已知100.00；明细来源日期2026-01-01，库龄250天。当前库存为零属于后续消耗，不用当前值替代历史。
7. 打开CCC说明，显示缺会计收入/成本期间确认及完整平均余额，“暂不可计算”。
8. 390×844 CSS视口验证页面、地图指标与贡献弹窗；1440×900也保存截图。截图工具会缩放输出，实际图像尺寸见screenshots.json。全页截图含地图，不把仅首屏截图声称地图全貌。
9. 切企业B，无360.00和CON-001残留，空数据可用。退出并完成Keycloak退出确认，受保护数据消失。期间也实际遇到会话到期，页面清空并重新经IdP建立会话。
10. 使用 carol / Fictional-carol-17! 登录企业A：仍可见合同360.00，资金和成本显示未授权，没有80.00/110.00泄漏；保存双视口权限说明。

以上是实际UI点击和输入，不是httpx替代。客户排行最后改为ID归组/编号标签的后端收尾由16项PG回归验证；浏览器截图来自该标签收尾前的同一实现运行进程，非独立Reviewer验证。

## 实际发现与修复

日期控件最初未响应浏览器input事件，截图initial-date-input保留；增加onInput后重新输入成功，并新增实际React生命周期回归。最早启动日志将合同打印成300.00（错误诊断文字），真实夹具含独立内存实际为360.00；已改为读取来源金额，原日志保留。未把这些失败描述为通过。

HTTP替身React组件另在 `/tests/analytics-component.html` 运行，7项通过：真实下钻点击、焦点撤权清理、企业迟到成功响应、刷新后迟到明细、快照变化、失败重试、日期输入。它们不冒充真实数据库或真实IdP。相关合同6/资金5/库存2/交付2/售后4项组件回归也通过。

截图和原始DOM见[evidence](evidence/screenshots.json)。视觉继承见[对照说明](visual-comparison.md)。

截图实际编码为JPEG，已按原字节改正扩展名，没有重新压缩或改图。部分中间截图名含390但实际为桌面，清单按实际像素标注；最终移动全页是mobile-verified-390.jpg，浏览器实测innerWidth390/innerHeight844。
