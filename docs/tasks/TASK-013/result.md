# TASK-013 执行结果

status: review_ready。等待独立审查，不自行accepted，不开始TASK-014。

## 提交与来源

- 已验收基线：`18966f4eaf3fdd1311b883e32f6f8e2eb123d48a`。
- 交接：`5a882909d6aee58b6f1262b5c2a91825a1618b22`。
- 编码前指标/ADR提交：`4fb5b02dd08d3156ee010cac23e45e9a5aa88a9a`。
- 实现提交：`a9bdbed5b320929eb686378d46169ad07eb5e9a2`。
- 最终HEAD：审查包 `REVIEW_MANIFEST.md` 从实际Git HEAD解析；不把自引用文档提交号伪造成已知值。
- main，保留原报告与TASK012结果/证据。TASK012 accepted基于用户及原增量报告，未改写Reviewer未重跑部分的限制。

## 交付

完成后端只读经营投影、带确认历史的兼容迁移0016、统一指标/贡献明细/快照契约、原工作空间驾驶舱与地图表格、资金/库存账龄及原业务跳转。后端Decimal，期间与截止日分离，冻结销售事实不按设备/付款重复放大；当前客户地区明确标注，未知与海外不丢失。

一致性采用事务首次查询前REPEATABLE READ、既有身份上下文与资金共享锁、内容快照校验。领域/行级/成本权限在总览与明细一致应用；撤权与企业切换清空报表，迟到成功响应不能恢复旧数据。确认时间元数据无金额，不另建交易台账。

客户排行最后按客户ID分组并显示编号、金额倒序，避免同名合并。对历史无法确定的资金确认时点返回部分/不可计算；标准财务周转率缺分子和平均余额时不可计算。库存区分所有权、阶段、质量，移库保留原龄；RMA部分返回及hold/scrap按实际处置区分。

## 实际验证

准确命令及证据见[validation.json](evidence/validation.json)。

**最终完整后端（包含真实OIDC）279 passed、22 warnings，365.92秒；最终代码上一次完整运行，无失败。** 原有弃用/字段alias警告保留，不通过升级依赖消除。下列较早轮次保留为过程记录。

- 完整非OIDC后端：277 passed / 11 warnings，338.57秒。涵盖原身份、CRM、报价、发布、合同、库存、装配、交付、资金、售后和Worker；此轮先于最后客户排行收尾。
- 最新分析真实PG/API：16 passed / 2 warnings，31.07秒。覆盖时间边界、历史冲销退款/更正、质保金、地区/分页、源权限/撤权、RLS、并发一致读、库存/未知成本/库龄、RMA、空库/保留0015升级及降级保护。最后客户排行收尾已包含。
- 上一轮包含真实OIDC的完整运行：276 passed、1 failed。真实OIDC通过；唯一失败是新增移库夹具使用错误路由，后续修正并通过。不把该轮称为全通过。
- React实际组件+HTTP替身：分析7、相关模块19，共26 passed。Node19 passed。类型检查、构建、OpenAPI/客户端重复生成一致性通过。
- 真实浏览器闭环、受限账号、企业切换及两视口见[浏览器记录](browser-acceptance.md)与[截图清单](evidence/screenshots.json)。使用真实IdP/API/独立PG，不关闭TLS；客户排行末次标签收尾由PG验证，截图运行进程早于该小改动。
- 代表性2000客户：固定50次查询（含身份/事务查询），一次0.233秒；原始EXPLAIN在analytics-final-candidate.txt。不是生产容量保证。
- 文档、差异、打包校验以final-checks及包内PACKAGE_CHECKS为准。远程CI未核实。

## 失败、限制与复现

保留首次404红测试、早期实现/夹具失败和全套原始输出。日期input交互失败已在实际浏览器修复并新增组件测试；最初浏览器种子诊断300.00写错，实际独立配置360.00正确，打印已改为读取来源。新增双客户测试先因PUB编号夹具重复失败，未称为产品缺陷复现；最终测试验证稳定身份归组。

自动审批曾拒绝“批量替换并关闭审计RLS”的组合操作，原因是范围未核实且可能削弱审计保护；该操作未执行。之后改为逐项补丁与有效成员上下文回填，审计RLS始终保留，所需本地测试正常获准。

- 旧数据无可靠确认审计或无有效成员时不能完整回填，历史指标明确不足；不伪造时间。详见[迁移边界](migration.md)。
- 每来源10万行保护限额，超过显式拒绝；当前同步批量投影无跨请求缓存。大规模数仓压测not_run。
- 库存的负责人范围/客户筛选不适用时明确显示；当前源页面跳转展示当前业务状态，历史贡献不会修改源事实。
- 地图为原生静态省级方格示意+真表格，不是行政边界导航。[来源和许可](../../analytics/regions-source.md)。
- 外部准备：锁定PG/Java/Keycloak、合法字体与本机可信证书；不打包私钥、会话、依赖或数据库。命令均从仓库根，环境路径用变量，见README及包内复现说明。
- 未部署、未执行业务自动化、未开始TASK014。生产拒绝启动保护不变。

[指标字典](../../analytics/metric-catalog.md) · [ADR018](../../architecture/adr/ADR-018.md) · [视觉变化](visual-comparison.md) · [变更清单](changed-files.txt)

新增原始日志中的pytest尾随空格和空行原样保存在gzip中；见[evidence/raw-log-index.json](evidence/raw-log-index.json)的解压后SHA256，没有清洗或改写失败输出。

## 独立审查 R1/R2 增量修复（2026-09-12）

status: review_ready，未自行accepted，不开始TASK014。审查基准`706e032cce1d944d1b6566388789bba82308884d`；本轮修复实现提交：`627b23c215c20a5ea86e1ea12cccb4f1e80cc73d`。最终HEAD由新审查包REVIEW_MANIFEST从实际提交解析。

原[独立报告](../../reviews/TASK-013-706e032-review.md)原样归档。R1未授权动态basis统一裁剪为通用说明；R2期间现金流不再被余额as_of截断，当前确认事实按发生日、逆向按登记日进入期间，余额继续保守历史重建。旧错误现金流断言修正，原资金和确认元数据未修改。

新增真实PG/API审查回归8项通过；完整非OIDC后端286项通过，真实OIDC单独执行；相关React8项、Node19项、类型检查、构建、接口生成一致性通过。命令与准确计数见[本轮验证](evidence/review-r1-r2/validation.json)。真实浏览器验证固定期间三种截止日、授权/无权限说明、企业切换及退出；桌面与移动截图已保存。见[修复与复现](review-repair.md)和[本轮差异文件](review-changed-files.txt)。

R1/R2红测试、绿测试、扩展夹具必填备注错误均保留在独立证据目录；未覆盖此前279项执行历史或Reviewer未独立重跑的限制。无关原浏览器/组件场景本轮not_run，理由为前端实现未改；本轮相关交互与完整后端已运行。远程CI未核实。完整增量包保留所有已跟踪源码、原报告、旧证据和本轮测试，SHA256清单校验，不含秘密、依赖或数据库。

本轮真实OIDC补充结果：1 passed，27.84秒；与非OIDC286项分开运行，合计覆盖完整后端集合。隔离栈已清理。
