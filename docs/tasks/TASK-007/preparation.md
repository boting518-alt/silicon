# TASK-007 实施前评估

status: planned

评估基准：59f55c813aba62ec8f18dc5d5e7cc649b277aeca；main，开始时工作树干净。任务书与用户附件逐字节一致。本文为实施准备与建议，不是独立审查通过、业务政策批准或 TASK-006 验收。

## 结论与前置

范围合理：衔接 TASK-006 的来源合同草稿，完成资料、付款约定、附件、线下签约登记与销售订单；签约/履行/实收状态分离，保持原报价商业内容、独立修订、租户权限及不可变历史。与 data-model、ADR-011 的后续边界一致。附件与签约竞争是本次主要工程风险，不能将本任务简化为合同表单。

本地 TASK-006 为 review_ready；已归档报告 TASK-006-0ca955a-review.md 结论 changes_requested，后续修复已交付，但尚未收到针对 59f55c8 的独立增量复核通过及负责人 accepted 记录。用户要求先判断再执行，附件又明确前置未满足只落仓准备，因此当前仅准备；不切换 runtime/current_task、AGENTS 或 TASK-006 状态，不实施 TASK-007/008。

启动恢复条件：取得明确对应 commit 的 TASK-006 增量审查通过依据与负责人 accepted；按任务书完成单独交接，再实施。不能从 GitHub 推送成功或本地测试通过推导 accepted。

## 需要明确的业务细节

1. **零金额合同**：目录/报价既有零价语义，而本任务要求每个付款节点正数且合计等于合同金额。建议正金额合同至少一个节点；零金额合同使用空计划并明确“无应付约定”，禁止伪造正数或零值节点；是否允许零金额签约由负责人明确。未明确时将零金额签约标为未配置并阻断，不能擅自禁止合法零价商品。
2. **付款触发**：建议首期仅实现明确枚举（签约日期、约定日、后续交付/验收事件等）。签约事件可由已登记事实推导；尚未接入的履行事件始终待触发。明确日历日/工作日、偏移范围和日期时区，首期可省略百分比辅助以避免额外尾差政策。
3. **主体与正式政策**：完善名称地址不得改变来源客户归属；更换法律主体不能暗中当作文字修正。区分来源中冻结的开发标志与登记时正式政策资格，开发记录不得转为正式记录；政策缺失/停用时阻断正式登记。签约日期未来值、必填字段及确认内容 hash 范围应在契约中明确，不新增法律有效性背书。

上述为准备建议，不冒称此前已批准规则；启动时可明确标注开发政策验证，不为缺少真实商务政策伪造可正式登记。

## 工程设计准备

- 0007_publication 的 contract_drafts 目前由权限及触发器整体保护不可变；不得直接取消保护来编辑其 content/source_hash。建议以现有 draft id 为唯一合同身份，增加一对一工作资料和付款/附件关系，保留原来源快照；签约生成独立不可变版本和订单。这是既有模型扩展，不另建平行合同来源。
- 签约预览 hash 应规范化并绑定资料、付款节点、就绪附件 id/hash/分类及来源，不包含观察时间等噪声；保存、附件变动与签约共享明确版本控制。签约事务重查源状态、对象权限和确认内容。幂等 operation 必须包含资源身份，保留 TASK-006 R1/R2 不变量。
- 锁顺序沿身份/成员 → catalog → quote → CRM 的现有顺序，再定义合同与附件锁的位置及统一顺序。所有编辑/签约/删除/清理路径遵守；签约与撤回通过既有 quote 锁排序，不能在合同锁后反向申请 quote 或成员锁。
- 文件存储与 PG 不具备跨资源原子事务：建议独立临时上传、验证完成后成为不可变对象、数据库关联提交后才可见；回滚遗留对象不可下载，按状态/年龄及当前引用重查清理。已签引用和补充材料分开，清理与签约互斥，不删除在途上传或已冻结文件。
- 本地私有存储采用环境变量相对路径，服务端随机 key，流式限制上传量、内容识别、扩展名允许表及 PDF/JPEG/PNG 限定；鉴权下载使用 attachment 与 nosniff，不公开目录。类型识别不等于恶意软件扫描，扫描缺失如实标记；原始文件名仅用于安全展示。
- 草稿可不完整、签约严格校验。合同冻结、唯一 sales_order 和审计在同一 PG 事务；商业/技术清单从签约快照复制，included 不重复收入，付款计划不变成收款记录。

## 准备阶段验证

只执行附件字节一致、文档链接和 git diff --check；不运行或宣称 TASK-007 实现、迁移、PG、浏览器验收。本任务保持 planned，后续实际执行另建 result.md，避免把准备误报为 review_ready。
