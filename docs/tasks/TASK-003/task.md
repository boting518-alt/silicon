# TASK-003：UI 保真与客户纵切

status: review_ready
owner: 执行线程
reviewer: 独立上下文，待指定

## 目标与前置

TASK-002 独立复核通过；两份 Demo 固定源码 checkout 可读且 blob 校验通过；开始前确认本次浏览器视觉测试授权。

必读：AGENTS.md、runtime/project.json、docs/design/demo-baseline.md、docs/architecture 下相关契约和 ADR、backlog 与前置任务 Review。

## 允许范围

原侧栏、页头、token 和相关 packages/ui；crm API/模型/迁移；客户列表详情表单及相应集成、E2E、截图。

## 实施要求

按 demo-baseline 保留工作空间布局、浅色面板与深绿强调，保持可识别交互。客户独立于合同：一客户多联系人、多项目、多合同与装机地点；客户侧项目负责人/关键人和内部销售/售后负责人分开并保留角色历史。实现新增、编辑、搜索、稳定分页与刷新持久化；表单后端验证且有并发版本检查；API 失败明确报错。其余入口显式标注仅演示或未启用，禁用未接入操作；不能假成功。固定 API 契约并生成客户端。

## 验收与实际证据

真实 PG CRUD，两个租户隔离；同客户关联多项目/联系人且模型预留多合同，不复制客户；创建/编辑后刷新 E2E 值保持；搜索、分页、重复提交和旧版本冲突。1440×900、390×844 同视口原 Demo/迁移页面对照；固定时钟、字体、虚构数据并关随机动画；键盘焦点、弹窗关闭/返回焦点、窄屏表格可用。截图差异逐项解释人工确认，不批量更新快照消除失败。

执行者必须将每个检查拆为可运行的实际命令，记录环境版本、退出码、摘要、证据相对路径及 not_run 原因。不要在尚无实现时伪造测试命令已经通过。

## 不在范围

不迁移完整报价/库存/合同系统，不替换模板，不把网页文字当源码；不修改或部署原 Demo。

## 退出、失败与交付

交付 scoped commit、base/head、diff、docs/tasks/TASK-003/result.md，状态 review_ready。独立 Reviewer 在相同内容 commit 复现检查，输出 docs/reviews/TASK-003-<head-short>.md。失败记录复现与修复，不进入下一任务；执行者不自行 accepted。涉及迁移先在测试库验证，不能以代码回滚代替数据恢复；不部署、不外发。
